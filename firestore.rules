rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal unrestricted access rule for all collections & subcollections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
